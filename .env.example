# Turso (libSQL) — used by api/register.ts on Vercel.
# Create a free database at https://turso.tech, then fill these in
# locally (.env, not committed) and in your Vercel project settings.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
